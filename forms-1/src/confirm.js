import React, { Component } from 'react';
import Form from './form.js';

export const Confirm = ({name}) => {
        return (
            <React.Fragment>
                <h2>{name}</h2>
            </React.Fragment>
        )
    
}
